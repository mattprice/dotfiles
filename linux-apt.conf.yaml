# This is a separate config file since apt-get requires sudo.
# It should be run before the main linux config.
# https://github.com/dein0s/dotbot_plugin_aptget

- aptget:
    - git
    - build-essential
    - fonts-firacode
    - zsh
